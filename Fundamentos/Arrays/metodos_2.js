const numeros = [1, 2, 3, 4, 5, 8];

numeros.splice(1, 2); // remover elemento do array. índice + quantidade de elementos a serem excluídos

console.log(numeros);

numeros.pop(); // remove último elemento do array 

console.log(numeros.pop());// mostra qual o elemento que foi excluído
console.log(numeros);