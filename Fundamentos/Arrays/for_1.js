const numeros = [1, 2, 3, 4, 5, 6, 11,45];

for(let i = 0; i < numeros.length; i+=2) { //numeros.lenght fez com que fosse percorrido todo o array, independente de quantos numeros
    console.log(numeros[i]);
}